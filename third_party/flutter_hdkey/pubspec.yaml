name: flutter_hdkey
description: hdkey
version: 0.0.2
author:
homepage:

environment:
  ##sdk: ">=2.2.2 <3.0.0"
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  convert: ^3.0.1
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.dabank.flutter_hdkey
        pluginClass: FlutterHDKeyPlugin
      ios:
        pluginClass: FlutterHDKeyPlugin
