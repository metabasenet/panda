part of trade_domain_module;

/// It indicates the side at which the order is placed.
/// - Bid (Buy) side indicates a downtick
/// - Ask (Sell) side indicates the uptick.
enum TradeSide {
  buy,
  sell,
}
