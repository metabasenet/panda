part of asset_ui_module;

class AssetMainTab extends StatelessWidget {
  const AssetMainTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return AssetListPage();
  }
}
